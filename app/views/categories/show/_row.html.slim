h3 = category

.category-coupons
  - if category.items.any?
    = render partial:'coupons/show/row', collection: category.items, as: :coupon 
  - else
    = render partial:'layouts/no_results', locals:{object: t('titles.empty_results.coupons'), in_profile: true}