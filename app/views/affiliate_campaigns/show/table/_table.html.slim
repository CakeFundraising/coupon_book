- if campaigns.any?
  #no-more-tables
    table#affiliate_campaigns.table.table-hover.table-bordered.table-striped.data-table
      thead.cf
        tr
          th = Affiliate.model_name.human(count: 2)
          th = AffiliateCampaign.human_attribute_name :total_donations
          th = AffiliateCampaign.human_attribute_name :total_donors
          th = AffiliateCampaign.human_attribute_name :community_rate
          th = AffiliateCampaign.human_attribute_name :campaign_rate
          th
      tbody
        = render partial:'affiliate_campaigns/show/table/row', collection: campaigns, as: :campaign
- else
  = render partial:'layouts/no_results', locals:{object: t('titles.empty_results.affiliates'), in_profile: true}