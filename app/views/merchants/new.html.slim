= display_meta("Account Setup")

.wizard-wrap
  .headline.text-center
    h1 Tell us about yourself
    h4.text-muted An organization, an individual or any good cause can raise money on Cake.
    br

  = semantic_form_for @user, url: registration_update_merchant_path(@user) do |f|
    = f.inputs do
      .row
        .col-md-12
          h3 Basic Information
          p.text-muted Add a picture, a contact phone, and your address.
          br
        .col-md-6
          = render partial:'avatar_pictures/form', locals:{f: f}
        .col-md-6
          = f.input :phone
          = f.input :url
          = render partial:'locations/form', locals:{f: f}
      br
      .text-center
        = f.actions do
          = f.action :submit, label: 'Save & Continue', button_html: {class: 'btn btn-success btn-lg'}