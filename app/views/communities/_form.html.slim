= f.semantic_fields_for :community, f.object.community || f.object.build_community do |c|
  = c.inputs do
    = c.input :slug, hint: 'Set a custom url where will be shown a promoter leaderboard and results for this Campaign.'
    = c.input :commission_percentage, as: :select, collection: commission_percentage, hint: 'Choose the percentage you offer to affiliates in order to participate.'
