nav.navbar.navbar-default role="navigation" 
  div.default class=(('logged-out' if current_user.nil?))
    .container
      /! Brand and toggle get grouped for better mobile display
      /! Collect the nav links, forms, and other content for toggling
      - if current_user.present?
        - unless current_fundraiser or current_sponsor
          .navbar-header
            a.navbar-brand
              = image_tag "efg_logo.png"
            button.navbar-toggle data-target="#navbar-collapse" data-toggle="collapse" type="button" 
              span.sr-only Toggle navigation
              span.icon-bar
              span.icon-bar
              span.icon-bar
        - else
          .navbar-header
            .logo
              = link_to root_path, class: 'navbar-brand' do
                = image_tag "efg_logo.png"
            button.navbar-toggle data-target="#navbar-collapse" data-toggle="collapse" type="button" 
              span.sr-only Toggle navigation
              span.icon-bar
              span.icon-bar
              span.icon-bar
        = render partial:'layouts/nav/fundraiser' if current_user.fundraiser?
        = render partial:'layouts/nav/sponsor' if current_user.sponsor?
      - else
        .navbar-header
          .logo
            = link_to efg_home_path, target: :_blank, class: 'navbar-brand' do
              = image_tag "efg_logo.png"
            button.navbar-toggle data-target="#navbar-collapse" data-toggle="collapse" type="button" 
              span.sr-only Toggle navigation
              span.icon-bar
              span.icon-bar
              span.icon-bar
        = render partial:'layouts/nav/default'          
        